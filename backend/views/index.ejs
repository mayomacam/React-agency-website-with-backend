<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Message</title>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">Message</h1>

    <% messages.forEach(message => { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-title"><%= message.name %></h4>
          <div class="card-subtitle text-muted mb-2">
            <%= message.sendedAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2"><%= message.email %></div>
          <a href="/<%= message.slug %>" class="btn btn-primary">Open message</a>
          <form action="/<%= message.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>